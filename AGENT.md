# Agent Guide

Le guide agent principal est maintenant dans `docs/AGENT.md`.

Ce fichier racine est conserve volontairement pour compatibilite avec les outils
qui recherchent prioritairement `AGENT.md` a la racine du repo.
